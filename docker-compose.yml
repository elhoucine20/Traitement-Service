version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: ./docker/php/Dockerfile
    container_name: app_container
    volumes:
      - ./:/var/www/html
    environment:
      APP_ENV: local
      APP_DEBUG: 'true'
      DB_CONNECTION: pgsql
      DB_HOST: postgres_db
      DB_PORT: 5432
      DB_DATABASE: rarecare
      DB_USERNAME: postgres
      DB_PASSWORD: 12345678
    ports:
      - "8000:8000"
    depends_on:
      - postgres_db

  nginx:
    image: nginx:alpine
    container_name: nginx_container
    ports:
      - "8080:80"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app

  postgres_db:
    image: postgres:16
    container_name: traitement_db_container
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: rarecare
      POSTGRES_PASSWORD: 12345678
    ports:
      - "5432:5432"
    volumes:
      - rarecare_data:/var/lib/postgresql/data

volumes:
  rarecare_data: